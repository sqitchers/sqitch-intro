[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
	# deploy_dir = deploy
	# revert_dir = revert
	# verify_dir = verify
	# extension = sql
# [core "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = /usr/local/pgsql/bin/psql
[user]
	name = Marge N. O’Vera
	email = marge@example.com
